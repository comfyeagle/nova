# Dependencies
node_modules/

# Build output (generated by `npm run build` / `npm run sync-android`)
dist/

# Native platform projects — generated locally via `npx cap add android|ios`.
# Each builder generates their own (with their own appId / signing), so they
# are not tracked here. See BUILDING.md.
android/
ios/

# Logs
*.log
npm-debug.log*

# OS / editor cruft
.DS_Store
Thumbs.db

# IDE — regenerated from Gradle on import
.idea/
*.iml
